node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Local VS Code workspace — copy fxo-web.code-workspace.eg to
# fxo-web.code-workspace and keep your own copy untracked.
/fxo-web.code-workspace

# Local agent/editor config (machine-specific)
.claude/
